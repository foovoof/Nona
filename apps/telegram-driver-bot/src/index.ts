/**
 * telegram-driver-bot — Telegram bot for drivers. Commands: /start, /online, /offline, /accept, /reject, /arrived, /complete.
 *
 * Entry point: initialize config → set up infrastructure → register handlers → start.
 */
console.log('telegram-driver-bot starting...');
// TODO: implement initialization
