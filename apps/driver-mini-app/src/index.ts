/**
 * driver-mini-app — Telegram Mini App for drivers. Map, earnings, availability.
 *
 * Entry point: initialize config → set up infrastructure → register handlers → start.
 */
console.log('driver-mini-app starting...');
// TODO: implement initialization
