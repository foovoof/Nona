/**
 * telegram-rider-bot — Telegram bot for riders. Commands: /start, /ride, /cancel, /status, /help.
 *
 * Entry point: initialize config → set up infrastructure → register handlers → start.
 */
console.log('telegram-rider-bot starting...');
// TODO: implement initialization
