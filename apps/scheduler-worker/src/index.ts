/**
 * scheduler-worker — Cron worker for scheduled tasks: scheduled rides, reports, cleanup.
 *
 * Entry point: initialize config → set up infrastructure → register handlers → start.
 */
console.log('scheduler-worker starting...');
// TODO: implement initialization
