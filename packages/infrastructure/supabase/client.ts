/** Supabase client factory. Only used by infrastructure adapters. */
export function createSupabaseClient(url: string, key: string) {
  throw new Error('Not implemented — requires @supabase/supabase-js');
}
