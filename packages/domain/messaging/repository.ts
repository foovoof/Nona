import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface MessagingRepository extends Repository<MessagingEntity, MessagingId> {
//   findByX(x: string): Promise<MessagingEntity[]>;
// }
