import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface DispatchRepository extends Repository<DispatchEntity, DispatchId> {
//   findByX(x: string): Promise<DispatchEntity[]>;
// }
