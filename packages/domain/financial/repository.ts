import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface FinancialRepository extends Repository<FinancialEntity, FinancialId> {
//   findByX(x: string): Promise<FinancialEntity[]>;
// }
