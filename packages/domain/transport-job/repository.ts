import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface TransportJobRepository extends Repository<TransportJobEntity, TransportJobId> {
//   findByX(x: string): Promise<TransportJobEntity[]>;
// }
