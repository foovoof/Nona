import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface IdentityRepository extends Repository<IdentityEntity, IdentityId> {
//   findByX(x: string): Promise<IdentityEntity[]>;
// }
