import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface SafetyRepository extends Repository<SafetyEntity, SafetyId> {
//   findByX(x: string): Promise<SafetyEntity[]>;
// }
