import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface GeoRepository extends Repository<GeoEntity, GeoId> {
//   findByX(x: string): Promise<GeoEntity[]>;
// }
