import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface PricingRepository extends Repository<PricingEntity, PricingId> {
//   findByX(x: string): Promise<PricingEntity[]>;
// }
