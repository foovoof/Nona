import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface SchedulingRepository extends Repository<SchedulingEntity, SchedulingId> {
//   findByX(x: string): Promise<SchedulingEntity[]>;
// }
