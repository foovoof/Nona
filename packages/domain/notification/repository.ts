import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface NotificationRepository extends Repository<NotificationEntity, NotificationId> {
//   findByX(x: string): Promise<NotificationEntity[]>;
// }
