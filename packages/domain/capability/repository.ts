import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface CapabilityRepository extends Repository<CapabilityEntity, CapabilityId> {
//   findByX(x: string): Promise<CapabilityEntity[]>;
// }
