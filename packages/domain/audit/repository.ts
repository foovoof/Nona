import type { Repository } from '@tos/shared/contracts';

// TODO: Extend with domain-specific query methods
// export interface AuditRepository extends Repository<AuditEntity, AuditId> {
//   findByX(x: string): Promise<AuditEntity[]>;
// }
